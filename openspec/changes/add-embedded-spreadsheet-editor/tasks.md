## 1. 方案与依赖
- [ ] 1.1 选型并接入开源离线电子表格引擎（默认：Univer）与必要的插件（公式/导入导出等）
- [ ] 1.2 封装 `SpreadsheetEditor` 组件：支持只读预览、编辑、获取当前文件字节/保存

## 2. 与 Notes 集成
- [ ] 2.1 `.xlsx` 打开：加载文件到 `SpreadsheetEditor`（取代当前 Handsontable）
- [ ] 2.2 `.xlsx` 保存：从 `SpreadsheetEditor` 导出为 `.xlsx`，写回文件系统
- [ ] 2.3 `.xlsx` 新建：创建空白工作簿并保存为 `.xlsx`，随后进入编辑

## 3. 互操作与兼容
- [ ] 3.1 导入兼容：WPS 导出的常见样式（合并、边框、填充、字体、对齐、行高列宽、数字格式）尽可能保真
- [ ] 3.2 导出兼容：保存后的文件可在 WPS 打开，核心布局与样式不明显漂移
- [ ] 3.3 建立样例库与回归手册：至少覆盖 20+ 个 WPS 真实文件

## 4. 性能与稳定性
- [ ] 4.1 大文件保护策略：避免 UI 卡死（懒加载/分页/上限策略/提示）
- [ ] 4.2 打开/切换/编辑的性能基线与监控点（不刷屏）

## 5. 清理与降级
- [ ] 5.1 移除或降级现有 Handsontable `.xlsx` 路径（保留开关以便回退）
- [ ] 5.2 更新文档与设置项（如有）
